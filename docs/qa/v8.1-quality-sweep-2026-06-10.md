# v8.1 quality sweep 2026-06-10

## Scope

- Pages checked: `/about-score`, `/operator`, `/contact`, `/privacy`, `/disclaimer`, `/video/J9QdxiZb8P4`, 404, top page, guide pages.
- Build checked with `npx next build`.
- Static output checked for internal page links and stale visible wording.

## Fixes applied

- Added shared next-action links to fixed policy pages via `PolicyPage`: top, roadmap, guide article.
- Added extra next-action links to `/about-score`: guide article and contact.
- Added custom `app/not-found.tsx` with Manapick branding, top/roadmap/guide links.
- Confirmed video detail already has score-method, YouTube, related videos, and list-return links; no layout change needed.

## Findings and decisions

- Old visible wording `8ジャンル`, `193本`, `公開中8` was not present in generated reader pages.
- Draft source files under `content/guide-draft-*` still contain historical `8ジャンル` wording, but these are source drafts and are not imported into public pages. No user-visible change needed.
- `掲載準備中` appears only in the public PR placeholder cards for guide pages without actual affiliate items. It is reader-facing placeholder text, not an internal implementation memo, so it was kept.
- Internal page-link scan over `out/**/*.html` found 0 missing internal page links after ignoring build assets and external URLs.
- JSON-LD script tags are present on checked fixed/video pages through common layout or page-level structured data.

## Build result

- `npx next build`: success.
- Static pages generated: 481.
